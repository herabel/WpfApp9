<Page x:Class="WpfApp9.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp9"
      mc:Ignorable="d" 
      d:DesignHeight="900" d:DesignWidth="1600"      Title="LoginPage" FontSize="26">

    <Grid Background="AliceBlue">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width=".3*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width=".3*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height=".3*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height=".3*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Логин"/>
            <TextBox Name="LoginTB"/>
            <TextBlock Text="Пароль"/>
            <PasswordBox Name="PasswordPB" Width="400"/>
            <Button x:Name="LoginBtn" Margin="0,10,0,0" Content="Войти" Click="LoginBtn_Click"/>
            <Button x:Name="LoginGuestBtn" Margin="0,10,0,0" Content="Войти как гость"/>
            <Button x:Name="RegBtn" Margin="0,10,0,0" Content="Регистрация" Click="RegBtn_Click"/>
        </StackPanel>
    </Grid>
</Page>
